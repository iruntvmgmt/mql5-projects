# Run manifest -- Phase 3 diagnostic run 1 (2025-01-06..07, TPV2 experimental) -- INVALIDATED

- **Purpose:** Phase 3 (`QuantBeast_Production_Readiness_Sprint.md`) --
  rerun the window containing a known fully-qualifying TP V2 episode
  (2025.01.06 07:15:00 SELL) with `InpEnableTPV2Experimental=true`, to
  observe whether it now flows through arbitration/risk/Shadow execution.
- **Git commit:** `27173da` (Phase 2 cardinality fixes)
- **Profile:** `Profiles/Tester/QuantBeast.TPV2ExperimentalShadow.20250106_20250107.ini`
  (new this session -- base profile
  `QuantBeastEA.XAUUSD.M5.20250106_20250107.400.ini` plus three appended
  `[TesterInputs]` lines: `InpTPV2_Enabled=true`,
  `InpEnableTPV2Experimental=true`, `InpTPV2_TriggerMode=0`, matching the
  exact serialization format of every other working boolean input in the
  same profile family).
- **Symbol:** XAUUSD, **Timeframe:** M5, **Model:** 4 (real ticks), Shadow mode.
- **Date range:** 2025-01-06 -- 2025-01-07.
- **SignalJournal byte range:** [42002112, 43107752)
- **TradeJournal byte range:** [205272, 207340)
- **Completion footer:** `240447 ticks, 276 bars generated`, `Test passed
  in 0:02:24.804`, `thread finished` -- **run itself completed naturally**,
  reproducing the exact tick/bar counts of the original evidence run.
- **INVALIDATED as evidence for Phase 3's actual question.** Reason: the
  startup config log (`Resolved Production Configuration`,
  `QBLogResolvedProductionConfiguration()`) explicitly recorded
  `TPV2Experimental=off` -- the profile's `InpEnableTPV2Experimental=true`
  line was not applied by the tester. The known-qualifying SELL episode at
  2025.01.06 07:15:00 still shows `TPV2_EXPERIMENTAL_DISABLED` in
  `SignalJournal.csv`, confirming the flag genuinely stayed off throughout
  the run, not merely a misread. See `DECISION_LOG.md` D009 for full
  diagnosis: this matches a previously-documented MT5 "new input not yet in
  the effective schema inside the tester until a terminal restart"
  pattern (`tp_displacement_matrix_20260722/README.md`).
- **Accepted/invalidated:** Invalidated (for Phase 3's purpose only -- the
  run's own natural-completion mechanics are fine and the .ini format is
  confirmed correct).
- **Broker orders transmitted:** none. Zero TP V2 (or any strategy)
  positions were affected by this run beyond the same organic FBO/MR
  Shadow activity already characterized in the original evidence run.
